const qs = (obj) => {
    const s = Object.entries(obj)
        .filter(([, v]) => v !== undefined && v !== null)
        .map(([k, v]) => `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`)
        .join("&");
    return s ? `?${s}` : "";
};
const handle = async (res) => {
    const text = await res.text();
    const json = text ? JSON.parse(text) : undefined;
    if (!res.ok) {
        const message = json?.error?.message ??
            json?.message ??
            res.statusText ??
            "Unknown Helius error";
        throw new Error(`Helius HTTP ${res.status}: ${message}`);
    }
    return json;
};
const makeEnhancedTxClientEager = (apiKey) => {
    const base = "https://api.helius.xyz/v0";
    const getTransactions = async ({ transactions, commitment, }) => {
        const url = `${base}/transactions` + qs({ "api-key": apiKey, commitment });
        const res = await fetch(url, {
            method: "POST",
            headers: { "content-type": "application/json" },
            body: JSON.stringify({ transactions }),
        });
        return handle(res);
    };
    const getTransactionsByAddress = async ({ address, before, until, commitment, source, type, limit }) => {
        const url = `${base}/addresses/${address}/transactions` +
            qs({
                "api-key": apiKey,
                before,
                until,
                commitment,
                source,
                type,
                limit,
            });
        const res = await fetch(url, { method: "GET" });
        return handle(res);
    };
    return {
        getTransactions,
        getTransactionsByAddress,
    };
};

export { makeEnhancedTxClientEager };
//# sourceMappingURL=client.eager.js.map
