import { createWebhook } from './createWebhook.js';
import { getWebhook } from './getWebhook.js';
import { getAllWebhooks } from './getAllWebhooks.js';
import { updateWebhook } from './updateWebhook.js';
import { deleteWebhook } from './deleteWebhook.js';

const makeWebhookClientEager = (apiKey) => ({
    create: (p) => createWebhook(apiKey, p),
    get: (id) => getWebhook(apiKey, id),
    getAll: () => getAllWebhooks(apiKey),
    update: (id, p) => updateWebhook(apiKey, id, p),
    delete: (id) => deleteWebhook(apiKey, id),
});

export { makeWebhookClientEager };
//# sourceMappingURL=client.eager.js.map
