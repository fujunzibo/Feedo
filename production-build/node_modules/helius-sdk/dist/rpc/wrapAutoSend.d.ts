import type { Rpc } from "@solana/kit";
export type AutoSent<T> = {
    [K in keyof T]: T[K] extends (...args: infer A) => infer R ? R extends {
        send: (...b: any[]) => infer S;
    } ? (...args: A) => S extends Promise<infer P> ? Promise<P> : Promise<S> : T[K] : T[K];
};
export declare const wrapAutoSend: <T extends Rpc<any>>(raw: T) => AutoSent<T>;
//# sourceMappingURL=wrapAutoSend.d.ts.map