import { getDeactivateInstruction } from '@solana-program/stake';
import { SYSVAR_CLOCK } from './types.js';

const makeGetUnstakeInstruction = () => (owner, stakeAccount) => getDeactivateInstruction({
    stake: stakeAccount,
    stakeAuthority: owner,
    clockSysvar: SYSVAR_CLOCK,
});

export { makeGetUnstakeInstruction };
//# sourceMappingURL=getUnstakeInstruction.js.map
