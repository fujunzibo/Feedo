import { getWithdrawInstruction } from '@solana-program/stake';
import { SYSVAR_STAKE_HISTORY, SYSVAR_CLOCK } from './types.js';

const makeGetWithdrawInstruction = () => (owner, stakeAccount, destination, lamports) => getWithdrawInstruction({
    stake: stakeAccount,
    recipient: destination,
    clockSysvar: SYSVAR_CLOCK,
    stakeHistory: SYSVAR_STAKE_HISTORY,
    withdrawAuthority: owner,
    args: lamports,
});

export { makeGetWithdrawInstruction };
//# sourceMappingURL=getWithdrawInstruction.js.map
