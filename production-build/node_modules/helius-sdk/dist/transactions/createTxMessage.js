import { pipe, createTransactionMessage, setTransactionMessageLifetimeUsingBlockhash, setTransactionMessageFeePayer, setTransactionMessageFeePayerSigner, appendTransactionMessageInstructions } from '@solana/kit';

const createTxMessage = ({ version, feePayer, lifetime, instructions, }) => {
    return pipe(createTransactionMessage({ version }), (m) => lifetime ? setTransactionMessageLifetimeUsingBlockhash(lifetime, m) : m, (m) => typeof feePayer === "string"
        ? setTransactionMessageFeePayer(feePayer, m)
        : setTransactionMessageFeePayerSigner(feePayer, m), (m) => appendTransactionMessageInstructions(instructions, m));
};

export { createTxMessage };
//# sourceMappingURL=createTxMessage.js.map
